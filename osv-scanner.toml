# OSV-Scanner suppressions. Every entry must document why the advisory is
# accepted and what would let us drop it. Revisit whenever the dependency tree
# changes (a new lockfile may remove the transitive path entirely).
#
# Currently empty, on purpose.
#
# The brace-expansion entry for GHSA-mh99-v99m-4gvg was dropped on 2026-08-03,
# following its own stated exit condition. Two things had changed:
#
#   1. A second, DIFFERENT advisory appeared, GHSA-rgw5-rvv9-x895, "DoS via
#      unbounded intermediate arrays, bypassing the CVE-2026-14257 mitigation".
#      It is a bypass of the first, not a duplicate, so the existing suppression
#      did not cover it and CI went red on the 1.13.0 release commit.
#   2. Unlike the original, this one has a patch on every line: 1.1.18, 2.1.4,
#      3.0.6, 5.0.9. The old entry's claim that no non-breaking fix existed was
#      true when written and is not true now. The 1.x and 2.x lines also
#      received backports for the ORIGINAL advisory (1.1.17 and 2.1.3), which
#      the old entry explicitly said did not exist.
#
# `npm update brace-expansion` moved the tree to 1.1.18 / 2.1.4 / 5.0.9, all of
# which are patched for BOTH advisories, so the suppression became stale.
# Suppressing an already-fixed vulnerability is worse than useless: it would
# hide a future regression on the same package.
#
# If a transitive dependency drags a vulnerable brace-expansion back in, the
# right response is another `npm update brace-expansion`, not a new entry here.
